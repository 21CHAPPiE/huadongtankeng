# Unified REST Gateway

Single FastAPI service that mounts:

- `HuadongCode` at `/huadong`
- `TanKengCode` at `/tanken`

Prepare and start from this directory:

```powershell
uv sync
uv run unified-rest
```
